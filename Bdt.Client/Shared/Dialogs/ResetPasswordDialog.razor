@using Bdt.Client.Shared.Dialogs;
@using Bdt.Shared.Dtos.WorkoutType;

<MudDialog>
    <DialogContent>

        <MudText Typo="Typo.body2" >Please privide your email below.</MudText>
        <br /><br />
        <MudTextField Placeholder="Email" @bind-Value="@Email" />

    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="MudBlazor.Color.Primary" OnClick="Next">Next</MudButton>
    </DialogActions>
</MudDialog>


@code {
    [CascadingParameter] MudDialogInstance? MudDialog { get; set; }

    private string? _email;
    [Parameter] public string? Email { get; set; }

    private bool isDisabled = true;

    private void Next()
    {
        MudDialog?.Close(DialogResult.Ok(Email));
    }

    private void Cancel()
    {
        MudDialog?.Close(DialogResult.Cancel());
    }
}